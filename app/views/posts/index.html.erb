<% if session[:user_id] %>
  <%= button_to "Create a Post", { action: "new" }, method: :get %>
<% end %>

<ul id="posts">
  <% @posts.each do |post| %>
    <li>
      <%= link_to(post.title, post_path(post)) %>
    </li>
  <% end %>
</ul>
